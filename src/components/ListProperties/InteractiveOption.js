import React from 'react'

const InteractiveOption = ({title}) => {
    return (
        <div className="selectCard">
            <h6 className="flatText">{title}</h6>
        </div>
    )
}

export default InteractiveOption
